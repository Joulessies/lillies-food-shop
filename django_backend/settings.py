INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    # ...other default apps
    'users',  # This is already there
    'lillies_backend',  # Add this line if it's missing
    # ...any other custom apps
]
